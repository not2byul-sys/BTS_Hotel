import streamlit as st
import pandas as pd
import json
import os

st.set_page_config(page_title="BTS 콘서트 숙소 찾기", page_icon="💜")
st.title("💜 BTS 콘서트 고양 숙소 실시간 리포트")

# 현재 위치의 파일 목록을 확인 (디버깅용)
file_path = 'korean_ota_hotels.json'

if os.path.exists(file_path):
    try:
        with open(file_path, 'r', encoding='utf-8') as f:
            data = json.load(f)
        
        if data and len(data) > 0:
            df = pd.DataFrame(data)
            st.success(f"✅ 총 {len(df)}개의 숙소 정보를 불러왔습니다.")
            st.dataframe(df, use_container_width=True)
        else:
            st.warning("⚠️ 데이터 파일(`json`) 안이 비어 있습니다. 맥북에서 스크래퍼를 실행해 주세요!")
    except Exception as e:
        st.error(f"❌ 데이터를 읽는 중 에러가 발생했습니다: {e}")
else:
    st.error(f"❌ 파일을 찾을 수 없습니다: {file_path}")
    st.info("현재 창고(GitHub)에 파일이 있는지 다시 한번 확인해 주세요.")